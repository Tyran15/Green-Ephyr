import React, { useState } from 'react';
import { View, Text, Button } from 'react-native';

export default function ConvMoedas({ navigation }) {
    const [valor, setValor] = useState(''); 

  return (
    <View>
        <Text>Hello world</Text>
    </View>
  );
}
